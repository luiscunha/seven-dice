packages:
  - "packages/*"

# pnpm >= 10 exige aprovação explícita dos pacotes que correm scripts de build.
# O esbuild é dependência do vite/vitest e sem isto o binário não é instalado.
allowBuilds:
  esbuild: true
